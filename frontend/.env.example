# Frontend Environment Variables
# Copy this file to .env.local and update with your values

# API Configuration
# The base URL for the FastAPI backend
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Prefect Dashboard URL (for ETL Pipeline Control link)
# The URL where Prefect dashboard is running (optional, used for ETL management links)
NEXT_PUBLIC_PREFECT_DASHBOARD_URL=http://localhost:4200
