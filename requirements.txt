# Core dependencies
# Python 3.13 compatible versions

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Database
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0  # Connection pooling for efficient DB access
psycopg2-binary>=2.9.9  # Required by Prefect for database migrations
asyncpg>=0.29.0  # Required by Prefect for async database operations
SQLAlchemy>=2.0.36

# Data processing (optional - comment out if not needed)
# pandas and numpy may have limited Python 3.13 support
# pandas>=2.2.3
# numpy>=2.1.0
# polars>=1.0.0

# Prefect - Modern workflow orchestration (Windows compatible)
prefect>=3.0.0
prefect-sqlalchemy>=0.5.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx>=0.27.0  # For testing FastAPI

# Data validation (HEAVY - comment out if not needed)
# great-expectations may not support Python 3.13
# great-expectations>=0.18.8

# Utilities
python-dotenv>=1.0.1
python-multipart>=0.0.12
rarfile>=4.0  # For RAR archive support

# Date handling
python-dateutil>=2.9.0

# Logging
loguru>=0.7.2

# Development
black>=24.0.0
flake8>=7.1.0
mypy>=1.13.0
isort>=5.13.2

# PDF Report Generation (optional)
reportlab>=4.2.0